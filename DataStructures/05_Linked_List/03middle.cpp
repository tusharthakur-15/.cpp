// return middle of a list
#include <iostream>
using namespace std;

/*
int middle(ListNode* head) {
    Node* slow = head;
    Node* fast = head;
    while(fast != NULL && fast->next != NULL) {
        slow = slow->next;
        fast = fast->next->next;
    }

    return slow;
}
*/

int main() {

    return 0;
}